color_scheme: dark
description: Low cost open source scanning tunneling microscope (STM) and atomic force microscope (AFM).
favicon: "assets/favicon.ico"
logo: "assets/logo text white.svg"
remote_theme: just-the-docs
theme: just-the-docs
title: LiSPM
url: https://yolisses.github.io/lispm
defaults:
  - scope:
      path: "" # Empty string target applies this rule to all files in the project
    values:
      layout: "default" # Replace "default" with your chosen layout name
